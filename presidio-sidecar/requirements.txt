# Runtime dependencies for the Presidio redaction sidecar.
#
# Pinned exactly. These were bare ">=" floors with no ceiling, so two builds of
# the same commit could install different code -- and a compromised or merely
# broken future release would land silently. Test-only packages live in
# requirements-dev.txt so they are not installed into the runtime image.

# FastAPI web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Microsoft Presidio for PII detection and anonymization
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364

# YAML configuration parsing
pyyaml==6.0.3

# File system watching for hot-reload
watchdog==6.0.0

# Large English model for PII detection accuracy (~500MB, official spaCy release)
en-core-web-lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.7.1/en_core_web_lg-3.7.1-py3-none-any.whl
